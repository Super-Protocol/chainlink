type = "offchainreporting"
schemaVersion = 1
name = "$JOB_NAME"
externalJobID = "$JOB_ID"
forwardingAllowed = false
maxTaskDuration = "0s"
blockchainTimeout = "20s"
contractAddress = "$JOB_CA"
contractConfigConfirmations = 3
contractConfigTrackerPollInterval = "1m0s"
evmChainID = "$EVM_CHAIN_ID"
isBootstrapPeer = $IS_BOOTSTRAP
p2pPeerID = "$P2P_PEER_ID"
p2pBootstrapPeers = $P2P_BOOTSTRAP_PEERS
# p2pv2Bootstrappers = $P2P_BOOTSTRAP_PEERS
keyBundleID = "$OCR_KEY_BUNDLE_ID"
observationTimeout = "10s"
transmitterAddress = "$TRANSMITTER_ADDRESS"
observationSource = """
    // Data source 1: CoinGecko
    ds1 [type=http method=GET url="https://api.coingecko.com/api/v3/simple/price?ids=bitcoin&vs_currencies=usd"];
    ds1_parse [type=jsonparse path="bitcoin,usd"];
    ds1_multiply [type=multiply times=100000000];

    // Data source 2: CryptoCompare
    ds2 [type=http method=GET url="https://min-api.cryptocompare.com/data/price?fsym=BTC&tsyms=USD"];
    ds2_parse [type=jsonparse path="USD"];
    ds2_multiply [type=multiply times=100000000];

    // Data source 3: Binance
    ds3 [type=http method=GET url="https://api.binance.com/api/v3/ticker/price?symbol=BTCUSDT"];
    ds3_parse [type=jsonparse path="price"];
    ds3_multiply [type=multiply times=100000000];

    // Pipeline connections
    ds1 -> ds1_parse -> ds1_multiply;
    ds2 -> ds2_parse -> ds2_multiply;
    ds3 -> ds3_parse -> ds3_multiply;

    // Median calculation from all sources
    ds1_multiply -> median;
    ds2_multiply -> median;
    ds3_multiply -> median;
    median [type=median];
"""
